from card.card import Card

class MCMCArranger:
    def __init__(self, cards: list(Card)):
        self.cards = cards
        
    def solve(seed: str = None):
        if (seed != None):
            return 1;
        else:
            return 0;